# Core MCP dependencies
mcp>=1.0.0

# HTTP client for API requests (compatible with MCP requirements)
httpx>=0.27.0

# Cryptography for secure token management
cryptography>=41.0.0

# Data validation and settings management
pydantic>=2.0.0

# Environment variable management
python-dotenv>=1.0.0

# Async file operations
aiofiles>=23.0.0

# JSON handling (usually built-in, but explicit for clarity)
# Standard library: json, base64, hashlib, pathlib, os, sys, asyncio, logging, typing

# Development and testing dependencies (optional)
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-mock>=3.10.0

# Code quality tools (optional)
flake8>=6.0.0
bandit>=1.7.0
safety>=2.3.0